export function filteredEmailTemplates (state, emailTemplates) {
    state.filteredEmailTemplates = emailTemplates
}

export function filteredInvoiceTemplates (state, invoiceTemplates) {
    state.filteredInvoiceTemplates = invoiceTemplates
}
